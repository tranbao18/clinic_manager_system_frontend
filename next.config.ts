import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // ESLint configuration removed - Next.js 16+ handles ESLint differently
  // Use package.json scripts or CLI for ESLint instead
};

export default nextConfig;
